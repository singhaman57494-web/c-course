//                         union

/*
what is union ?

1. union is a user define data type (vey similar to structures)

2. the difference between structures and unions line in the fact that in structure , each number has its own 
   storage location, whereas members of a union uses a single shared memory location.
   
3. this single shared memory location is equal to the size of its largest data member.

declaring and accessing union members.

1. like structure, we access any memeber  by using the member access operator (.) in unions.

2. we use keyboard union to define a union.

3. syntax is very similar to that of structure.

*/