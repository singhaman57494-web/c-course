//                                   definition of pointer 

/*
question:-                        what is pointer ?

1. variable which stores the address of another variable.

2. can be of type int , char , arrey,, function, or any other pointer .

3. size depends on the architecture. ex 2 bytes of 32 bit.

4. pointer in c programming language can be declaeed using *(asterisk symbol).




/                            & and * operators

1. the address of operator '&' return the address of a variable.

2. '*' is the deference operator (also called INDIRECTION OPERATOR) used to get the value
   at a given address.


//                            NULL pointer

1. a pointer that is not assigned any value but NULL is known as the NULL pointer.

2. in computer programming , a NULL pointer is a pointer that does not point to any object 
   or function .

3. we can us it to initialize a pointer variable when that pointer variable isn't assigned any 
valid memory address yet.

int *ptr = NULL;

//                             uses of pointer 

1. dynamic memory allocation 
2. arrays, functions, and structures.
3. return multiple value from a function.
4. pointer reduces the code and improves the performance.


*/