#include <stdio.h>

/*  WHAT IS variable?
a variable is essentially a name given to a storage location in memory, which is used to hold or store values that can be processed during the execution of a program.
Think of it as a container where you can keep data—like numbers or characters—so that your program can access and manipulate that information later */

//    example :- sum of 2 numbers

int main ()
{
    int a = 20;
    int b = 20;
    int sum = a + b;

    printf("%d", sum);

    return 0;
}