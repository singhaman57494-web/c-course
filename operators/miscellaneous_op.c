# include <stdio.h>

//          miscellaneous operator
/*
 operator     |                discription                   |     example

 sizeoff      | return the size of variable                  |sizepf(a), where a is integer, will return int's
                                                             |size on that architecture

 &            | return the address of a variable             | &a; returns the actual address of the variable.

 *            | pointer to a variable                        | *a;

 ?;           | conditional expression                       | if condition is true ? then value X: otherwise 
                                                             |value y 
                
*/