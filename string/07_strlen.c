//                         lenth of string

#include <stdio.h>
#include <string.h>


int main()
{
    char s1[] = "harry";
    char s2[] = "ravi";

    printf("the lenth of s1 is %d\n", strlen(s1));
    
    int len = strlen(s2);
    printf("the length of s2 is %d\n", len);
    return 0;
}